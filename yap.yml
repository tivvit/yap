version: 1.0
settings:
  state:
    type: json
    file: state.json
pipeline:
  main:
    include: example.py
    type: script
  test:
    include: test.yml
    deps:
      - main
  finalize:
    exec: echo "hello"
    deps:
      - test